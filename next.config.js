/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Optionnel : décommenter si vous utilisez Docker
  // output: 'standalone',
}

module.exports = nextConfig

